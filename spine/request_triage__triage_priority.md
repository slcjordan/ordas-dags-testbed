# What `ordas__request_triage__triage_priority` will do

GENERATED BY ORDAS. Do not edit: this file is a restatement of what runs,
so an edit here changes the description and not the behaviour, and the two
would then disagree with nothing to catch it. That is a stricter rule than
the one on the DAG beside it, where an edit is measured rather than
discouraged: editing a schedule changes what happens, and editing this
changes only what a reviewer believes happens.

`dags/request_triage__triage_priority.py` is a schedule and says nothing
about the work. This file is the work. Reviewing the DAG without reading
this one is approving a `bash_command`.

- process: `request-triage`, segment 1 of 1
- step: `request-triage/1/triage-priority`
- acts as: `root/servicedesk/triage`
- declared read set, prefetched at this segment's t0:
  - `jsm:Issue/{issue_key}`

## What it may change

The upper bound on what this segment may change, across every branch it
can take. A reviewer does not get to choose which branch a ticket takes,
so this is the union and not the common case.

- `jsm:Issue/{issue_key}` — may set status
  - resolve a low-priority request, or park anything above it for the queue
- `jsm:Issue/{issue_key}/comment` — may set body
  - say what was decided, in words, on the ticket

Declared rather than enforced: nothing at run time consults this list, and
the check that it is true is a test that drives the segment and compares
what it wrote against what it said. That catches drift in our CI rather
than in your Jira, which is better than nothing and is not the same as an
enforced permission.

## There is no spine to review

This segment is hand-written Go, not a promoted spine: `ordasrun -candidate triage-priority` calls a function in internal/candidate directly, and there is no rendered handler to describe.

**What this means for the review.** You can see WHAT this segment touches:
the read set and the write set above are both declared, and together they
bound the blast radius. What you cannot see is WHEN and WHY -- which
branch it takes, in what order it writes, and what decides between
resolving a request and parking it for a person. That is what a rendered
spine shows and what a hand-written segment has no way to say.

So approving this bundle means accepting the bound above without seeing
the reasoning inside it. Whether that is enough is a judgement about how
much the write set can hurt, which is exactly the judgement a reviewer is
qualified to make and could not make from four files that never mentioned
it.
