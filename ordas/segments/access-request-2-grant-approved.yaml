# access-request-2-grant-approved.yaml -- segment 2 of 2 of access-request.
#
# GENERATED BY ORDAS. Do not edit.
#
# Editing is not forbidden, it is measured. source_sha256 hashes the INPUTS
# this was generated from; the evidence block beside it hashes this file's
# CONTENTS. An edit that moves one and not the other is a hand-edit, and that
# is the number Gate C is asking for. Editing it anyway and saying so is more
# useful than a clean regeneration that hides what had to change.
#
# THIS FILE DOES NOT SAY WHAT THE WORK IS. It says which work, and the bound on
# what the work may change. What the segment actually does is described in
#
#     spine/access_request__grant_approved.md
#
# and approving this without reading that is approving an identity.
#
# It is data. There is no run:, no if:, no uses: and nowhere to put one -- the
# schema below is the whole vocabulary. What executes it is a workflow you
# wrote, in your repository, that ORDAS cannot edit.

schema: "ordas/segment/1"

emitter: "emit/1"
source_sha256: "f61028e76d07f91e44b609b7d502f1e0abd57b406b79f5fdc12bb6761f79fd8d"

process: "access-request"
unit: "grant-approved"
segment: 2
segment_count: 2
step_id: "access-request/2/grant-approved"
scope: "servicedesk/access-request/grant-approved"
actor: "root/access/segment"

resume_on: "Approved"
parks_at: ""

reads:
  - "jsm:Issue/{issue_key}"

# Declared, not enforced: nothing at run time consults this. It is the bound a
# reviewer has on what the segment may change other than our word for it.
writes:
  - template: "jsm:Issue/{issue_key}"
    fields: ["status", "customfield_granted"]
    why: "resolve the request at the level that was approved"
  - template: "jsm:Issue/{issue_key}/comment"
    fields: ["body"]
    why: "say what was granted, in words, on the ticket"

envelope:
  required: ["issue_key"]

command: "ordasrun"
timeout_seconds: 120
broker_url: "http://ordas-broker.internal:8080"
