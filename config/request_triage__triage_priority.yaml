# Generated by ordas. The record of what dags/request_triage__triage_priority.py was generated from.
#
# NOTHING READS THIS AT RUN TIME, and that is a limitation rather than a
# design. The DAG is a pure function of these values, so a change here needs a
# regeneration to take effect -- editing this file alone changes nothing. It is
# recorded anyway because a reviewer should be able to see the operational
# choices without reading Python, and because a regeneration that disagrees
# with this file is a question worth asking.
#
# An edit here and an edit to dags/ mean different things. This one is a
# request the emitter does not yet answer, and it is visible. That one is a
# divergence, and only the provenance hash below finds it.

emitter: "emit/1"
source_sha256: "a49205192d244b5895b5225a8a9a0f39e8118c31a4a576471825ba77aa7388e0"

process: "request-triage"
segment: 1
segment_count: 1
step_id: "request-triage/1/triage-priority"
unit: "triage-priority"
scope: "servicedesk/request-triage"
actor: "root/servicedesk/triage"
reads:
  - "jsm:Issue/{issue_key}"
writes:
  - template: "jsm:Issue/{issue_key}"
    fields: ["status"]
    why: "resolve a low-priority request, or park anything above it for the queue"
  - template: "jsm:Issue/{issue_key}/comment"
    fields: ["body"]
    why: "say what was decided, in words, on the ticket"

airflow: "2"
python: "3.12"
broker_url: "http://ordas-broker.internal:8080"
start_date: "2026-01-01"
retries: 2
retry_delay_seconds: 60
max_active_runs: 8
timeout_seconds: 120
ordasrun: "ordasrun"
